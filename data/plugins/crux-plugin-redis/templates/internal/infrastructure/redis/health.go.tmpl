// Package redis — health check registration for the Redis dependency.
// Generated by crux {{ .meta.cli_version }} — crux-plugin-redis@1.0.0.
package redis

import (
	"context"

	"{{ .service.module }}/internal/domain/health"
)

// RegisterHealth registers the Redis ping as a readiness dependency.
func (c *Client) RegisterHealth(registry *health.Registry) {
	registry.Register("redis", func(ctx context.Context) error {
		return c.Ping(ctx)
	})
}
