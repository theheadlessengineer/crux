# Generated by crux {{ .meta.cli_version }} — crux-plugin-terraform-azure@1.0.0
terraform {
  required_version = ">= 1.5.0"
  required_providers {
    azurerm = {
      source  = "hashicorp/azurerm"
      version = "~> 3.0"
    }
  }
}

provider "azurerm" {
  features {}
  subscription_id = var.subscription_id
}

module "azure_database" {
  source          = "git::https://github.com/company/platform-terraform//modules/azure-database?ref=v1.0.0"
  location        = var.location
  resource_group  = var.resource_group
  service         = var.service_name
  env             = var.environment
}

module "redis_cache" {
  source         = "git::https://github.com/company/platform-terraform//modules/azure-redis?ref=v1.0.0"
  location       = var.location
  resource_group = var.resource_group
  service        = var.service_name
  env            = var.environment
}
