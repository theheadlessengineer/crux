{
  "permissions": {
    "allow": [],
    "deny": [
      "Bash(rm -rf *)",
      "Bash(git push --force)"
    ]
  },
  "env": {
    "SERVICE_NAME": "{{ .service.name }}",
    "ENVIRONMENT": "development"
  }
}
