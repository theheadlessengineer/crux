apiVersion: v1
kind: Service
metadata:
  name: {{ .service.name }}
  labels:
    app: {{ .service.name }}
spec:
  selector:
    app: {{ .service.name }}
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
