// Package kafka — health check registration for the Kafka dependency.
// Generated by crux {{ .meta.cli_version }} — crux-plugin-kafka@1.0.0.
package kafka

import (
	"context"

	"{{ .service.module }}/internal/domain/health"
)

// RegisterHealth registers the Kafka broker ping as a readiness dependency.
func (c *Client) RegisterHealth(registry *health.Registry) {
	registry.Register("kafka", func(ctx context.Context) error {
		return c.Ping(ctx)
	})
}
