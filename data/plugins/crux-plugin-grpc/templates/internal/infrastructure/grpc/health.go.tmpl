// Package grpc — health check for the gRPC server.
// Generated by crux {{ .meta.cli_version }} — crux-plugin-grpc@1.0.0.
package grpc

import "context"

// Ping checks that the gRPC server is running — used by the health registry.
func (s *Server) Ping(_ context.Context) error {
	// gRPC health is managed via grpc_health_v1 — this is the HTTP health bridge.
	return nil
}

// HealthCheck returns a named health check function for the gRPC server.
func (s *Server) HealthCheck() (string, func(context.Context) error) {
	return "grpc", s.Ping
}
