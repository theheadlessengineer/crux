# {{ .service.name }}

> Generated by [crux](https://github.com/theheadlessengineer/crux) {{ .meta.cli_version }} on {{ .meta.generated_at }}.
> **Read [TODO.md](TODO.md) before writing any business logic.**

---

## Quick Start

```bash
make dev        # format → vet → lint → test
make build      # compile binary to ./bin/{{ .service.name }}
make run        # build and run locally
```

## Health Endpoints

| Endpoint | Purpose |
|---|---|
| `GET /health` | Aggregate dependency health |
| `GET /ready` | Readiness probe (K8s) |
| `GET /live` | Liveness probe (K8s) |
| `GET /metrics` | Prometheus scrape endpoint |
| `GET /version` | Service version and build info |

## Configuration

All configuration is via environment variables:

| Variable | Default | Description |
|---|---|---|
| `SERVICE_NAME` | `{{ .service.name }}` | Service identifier in logs and traces |
| `ENVIRONMENT` | `development` | Runtime environment |
| `PORT` | `8080` | HTTP listen port |
| `LOG_LEVEL` | `info` | Log level: debug, info, warn, error |
| `OTEL_EXPORTER_OTLP_ENDPOINT` | `localhost:4317` | OTel collector endpoint |
| `CORS_ALLOWED_ORIGINS` | _(deny all)_ | Comma-separated allowed origins |
| `SHUTDOWN_TIMEOUT_SECONDS` | `30` | Graceful shutdown drain timeout |

## Tier 1 Standards Applied

- Structured JSON logging (W3C traceparent correlation)
- OpenTelemetry tracing (OTLP/gRPC export)
- RFC 7807 error responses
- Graceful shutdown (SIGTERM/SIGINT)
- Security headers (CSP, HSTS, X-Frame-Options)
- Input sanitization (path traversal, null bytes, body size)
- Non-root Dockerfile (distroless)
- Read-only root filesystem (K8s securityContext)
- Default-deny network policies

## Plugins Used

{{ range .plugins_used }}- {{ . }}
{{ end }}

## Team

**{{ .cost.team }}** — see [catalog-entry.yaml](catalog-entry.yaml) for ownership details.
