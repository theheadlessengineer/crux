#!/usr/bin/env bash
# snapshot-db.sh — Capture current local database state.
# Generated by crux {{ .meta.cli_version }}. Replace with real snapshot logic.
set -euo pipefail

SNAPSHOT_DIR=".db-snapshots"
SNAPSHOT_NAME="${1:-$(date +%Y%m%d-%H%M%S)}"

mkdir -p "$SNAPSHOT_DIR"
echo "TODO: add snapshot logic for {{ .service.name }} → $SNAPSHOT_DIR/$SNAPSHOT_NAME"
