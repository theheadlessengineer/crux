# data-classification.yaml — generated by crux
# Declare all PII/PHI fields handled by this service.
# The @company/logger reads this file to redact declared fields from log output.
# The @company/ai-client reads this file before sending data to AI APIs.
#
# Classification levels: public | internal | confidential | restricted
#
# TODO (required before production):
#   - List every field that contains PII or PHI.
#   - Choose the correct classification level for each field.
#   - Choose a redaction strategy: mask | hash | drop
#   - Review with your Data Protection Officer if handling PHI.

service: {{ .service.name }}
team: {{ .cost.team }}

fields: []
# Example — uncomment and adapt:
# fields:
#   - name: email
#     classification: confidential
#     redaction: mask        # mask | hash | drop
#     description: "User email address"
#
#   - name: phone_number
#     classification: confidential
#     redaction: mask
#     description: "User phone number"
#
#   - name: national_id
#     classification: restricted
#     redaction: drop
#     description: "Government-issued ID — never logged"
