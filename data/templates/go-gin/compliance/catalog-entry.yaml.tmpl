# catalog-entry.yaml — generated by crux
# Service catalog metadata consumed by the platform's service registry.
# Keep this file up to date — it is the authoritative source for service ownership.
#
# TODO (required before production):
#   - Fill in owner, on_call, and slack_channel.
#   - Add all upstream and downstream dependencies.
#   - Set the lifecycle stage correctly.

service: {{ .service.name }}
team: {{ .cost.team }}
description: "TODO: describe what this service does in one sentence."

ownership:
  owner: "TODO: owner@company.com"
  on_call: "TODO: on-call rotation name or PagerDuty service ID"
  slack_channel: "TODO: #team-channel"
  runbook: "docs/runbook.md"

lifecycle:
  # draft | active | deprecated | decommissioned
  stage: draft

api:
  # REST | gRPC | event-driven | internal
  type: REST
  spec: "docs/openapi.yaml"

dependencies:
  upstream: []   # TODO: services this service calls
  downstream: [] # TODO: services that call this service

tags:
  environment: {{ .service.environment }}
  cost_centre: {{ .cost.centre }}
