# Application configuration for {{ .service.name }}.
# Generated by crux {{ .meta.cli_version }}.

import os
from functools import lru_cache


class Settings:
    service_name: str = os.getenv("SERVICE_NAME", "{{ .service.name }}")
    environment: str = os.getenv("ENVIRONMENT", "development")
    port: str = os.getenv("PORT", "8080")
    log_level: str = os.getenv("LOG_LEVEL", "INFO")
    version: str = os.getenv("VERSION", "dev")
    commit: str = os.getenv("COMMIT", "unknown")
    build_time: str = os.getenv("BUILD_TIME", "unknown")


@lru_cache
def get_settings() -> Settings:
    return Settings()
