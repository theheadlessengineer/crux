{
  "name": "{{ .service.name }}",
  "version": "0.0.1",
  "description": "{{ .service.name }} — generated by crux {{ .meta.cli_version }}",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "node --test tests/",
    "lint": "eslint ."
  },
  "dependencies": {
    "express": "^4.19.0"
  },
  "devDependencies": {
    "eslint": "^9.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
