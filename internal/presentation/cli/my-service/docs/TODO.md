# TODO — my-service

Generated by crux dev on 2026-03-18T14:33:55Z.

**Read this file before writing any business logic.**

The items below are intentional placeholders left by crux. Each one must be
resolved before this service goes to production. Check off items as you complete
them and delete this file when all items are done.

---

## Required before production

- [ ] **cost-budget.yaml** — fill in `monthly_budget_usd` and per-component breakdown.
- [ ] **data-classification.yaml** — declare all PII/PHI fields handled by this service.
- [ ] **catalog-entry.yaml** — fill in `owner`, `on_call`, and `slack_channel`.
- [ ] **docs/runbook.md** — fill in RTO/RPO targets and escalation path.
- [ ] **docs/capacity-model.md** — complete all traffic and resource estimates.
- [ ] **slo.yaml** — confirm availability and latency targets with your team.
- [ ] **resilience.yaml** — review and calibrate timeout, retry, and circuit breaker values.
- [ ] **docs/adr/ADR-001** — review auto-generated ADR and add any missing context.

## Required before first deployment

- [ ] Set `SECRETS_BACKEND` environment variable (`vault` or `aws-secrets-manager`).
- [ ] Wire the Vault or AWS Secrets Manager SDK in `internal/infrastructure/secrets/`.
- [ ] Add required secret keys to the `Load()` call in your service entrypoint.
- [ ] Confirm `/health`, `/ready`, and `/metrics` endpoints return correct responses.
- [ ] Verify structured JSON logs contain `traceId` and `correlationId` fields.
- [ ] Run `make dev` — all tests must pass before the first PR.

## Recommended before go-live

- [ ] Run a load test and update `docs/capacity-model.md` with real numbers.
- [ ] Import `infra/monitoring/dashboard.json` into Grafana and verify all panels.
- [ ] Validate `infra/monitoring/alerts.yaml` with `promtool check rules`.
- [ ] Review `catalog-entry.yaml` lifecycle stage — change from `draft` to `active`.
- [ ] Complete a production readiness review with the platform team.
