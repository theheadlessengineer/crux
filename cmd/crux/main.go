// Package main is the entrypoint for the crux CLI tool.
package main

var version = "dev" // Set via ldflags at build time

func main() {
	_ = version // Used by CLI in Epic 1.2
	// Placeholder entrypoint - CLI implementation in Epic 1.2
}
